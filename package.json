{
  "name": "petanque-turnier-manager-online",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "git config core.hooksPath .githooks || true",
    "dev": "npm run build && npm run db:migrate:local && wrangler dev --ip 0.0.0.0",
    "dev:vite": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run quality-gate && npm run db:migrate:remote && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "security:check": "node scripts/security-check.mjs && node scripts/i18n-check.mjs",
    "i18n:check": "node scripts/i18n-check.mjs",
    "test": "node scripts/formation-registration-type-check.mjs && node scripts/timezone-check.mjs && node scripts/email-cancel-link-check.mjs && node scripts/email-footer-check.mjs",
    "quality-gate": "npm run build && npm test && npm run security:check"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tz-lookup": "^6.1.25"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "wrangler": "^4.0.0"
  }
}
