export default function App() {
  return (
    <main className="page">
      <section className="hello" aria-labelledby="hello-title">
        <p className="eyebrow">Cloudflare React Starter</p>
        <h1 id="hello-title">Hello World</h1>
        <p className="subtitle">Clean restart for a new project.</p>
      </section>
    </main>
  );
}
